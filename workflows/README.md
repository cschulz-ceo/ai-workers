# n8n Workflows

Version-controlled exports of all n8n workflows. These JSON files are the canonical
source of truth — import them into n8n to restore or replicate the automation stack.

## Workflow Index
(Add entries as workflows are built)

| Filename | Description | Trigger |
|----------|-------------|---------|
| (none yet) | | |

## Export / Import
- Export: n8n UI → Workflow → Download
- Import: n8n UI → Import from file
- Keep exports updated whenever a workflow changes (can be automated via MCP)
